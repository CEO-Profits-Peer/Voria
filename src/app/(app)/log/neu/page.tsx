import { NeueReiseFormular } from '@/features/log/NeueReiseFormular';

export const metadata = { title: 'Neue Reise · Voria' };

export default function NeueReiseSeite() {
  return <NeueReiseFormular />;
}
